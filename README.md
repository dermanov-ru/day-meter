# DayMeter

**DayMeter** — приватный сервис для ежедневной фиксации состояния, поведения и контекста жизни.  
Минималистичный, расширяемый и ориентированный на осмысленный анализ — в том числе с помощью ИИ.

Проект создавался как личный инструмент, но поддерживает многопользовательский режим
(регистрация и авторизация), поэтому может использоваться и другими людьми.

---

## 🎯 Идея проекта

DayMeter решает простую, но важную задачу:

> **Дать человеку честные и структурированные данные о своей жизни,  
> не навязывая интерпретаций, оценок и “правильных выводов”.**

Сервис:
- не даёт советов,
- не оценивает «хорошо / плохо»,
- не занимается геймификацией.

Он **собирает факты и контекст**, которые можно:
- перечитать со временем,
- проанализировать за месяц или год,
- экспортировать и передать ИИ для глубокой аналитики.

---

## 🧱 Основные возможности

### 1. Метрики

DayMeter поддерживает расширяемую систему метрик.

**Типы метрик:**
- `boolean` — Да / Нет
- `scale` — числовая шкала (например, 1–10)

Примеры метрик:
- настроение
- раздражение
- утомила работа
- работал ночью
- гулял с детьми
- болел
- был на массаже

Метрики:
- добавляются и настраиваются через UI,
- имеют фиксированный тип и диапазон,
- могут быть активированы или скрыты без потери данных.

---

### 2. Категории метрик

Метрики всегда принадлежат категории (одной).

Примеры категорий:
- Дом / семья / дети
- Работа
- Энергия
- Здоровье
- Эмоции
- Прочее (системная категория по умолчанию)

Категории используются для:
- группировки метрик в UI,
- улучшения читаемости форм,
- структурирования экспорта.

Поддерживаются:
- системные категории (включение / выключение),
- пользовательские категории (создание, редактирование, удаление при отсутствии метрик).

---

### 3. Ввод данных за день

**Страница:** `/entry/{date}`

Особенности:
- метрики сгруппированы по активным категориям;
- boolean → чекбоксы;
- scale → удобные слайдеры (range input);
- комментарии к отдельным метрикам (опционально);
- заметка дня — свободный текст (контекст всего дня).

Поддерживается **логическая дата дня**:
- если ввод происходит ночью (до cutoff-времени),
  данные автоматически относятся к предыдущему дню.

---

### 4. Хроника (летопись)

**Страница:** `/chronicle`

Функциональность:
- хронологический список дней (новые сверху);
- для каждого дня:
    - заметка дня;
    - метрики, сгруппированные по категориям;
    - комментарии к метрикам (помечены 💬).

Это не аналитика, а **читаемая история жизни**.

---

### 5. Экспорт для ИИ и анализа

DayMeter поддерживает экспорт летописи в **Markdown**.

Возможности:
- выбор периода (месяц, квартал, год и др.);
- стабильный текстовый формат;
- данные можно целиком скопировать и передать в ИИ (ChatGPT, Claude и т.д.).

Экспортируемые данные:
- даты и дни недели;
- заметки дня;
- метрики по категориям;
- комментарии к метрикам.

Проект сознательно **не делает автоматический анализ** —
анализ предполагается внешним инструментом (ИИ или человек).

---

### 6. Управление через UI (без деплоя)

Из кабинета можно:
- добавлять метрики;
- выбирать категорию метрики;
- управлять активностью и порядком;
- добавлять и настраивать категории.

Это позволяет адаптировать сервис «с дивана»,
без изменения кода и повторного деплоя.

---

## 🧠 Архитектурные принципы

- Laravel + Blade (без SPA)
- нормализованная модель данных (без JSON-полей)
- все изменения схемы — только через миграции
- строгие ключи (`metric.key`, `category.key`) — immutable
- soft-архивирование через `is_active`, без удаления данных

Проект ориентирован на:
- расширяемость,
- читаемость,
- долгосрочное использование.

---

## 🔐 Приватность

- стандартная регистрация и авторизация;
- все данные привязаны к пользователю;
- нет публичных профилей и шаринга;
- сервис не отправляет данные во внешние системы.

---

## 🧪 Для кого этот проект

DayMeter подойдёт тем, кто:
- хочет осмысленно отслеживать свою жизнь;
- устал от трекеров с «бейджами» и оценками;
- хочет использовать ИИ как инструмент анализа, а не магию;
- ценит простые и честные системы.

---

## 🏁 Коротко

> **DayMeter — это личная хроника жизни,  
> достаточно структурированная для ИИ  
> и достаточно человеческая для самого пользователя.**

---

## 📌 Статус

Проект находится в активной разработке и используется в реальной жизни.  
Архитектура готова к аккуратному расширению без ломки основы.


# Tools

```
./vendor/laravel/sail/bin/sail up -d
./vendor/laravel/sail/bin/sail artisan migrate
./vendor/bin/sail npm run build

./vendor/bin/sail artisan chronicle:export --from=2026-01-01 --to=2027-01-01 --user-id=2
```
