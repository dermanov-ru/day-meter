# DayMeter

**DayMeter** — минималистичный приватный сервис для ежедневной фиксации состояния, поведения и нагрузки.  
Проект сделан как простой, надёжный инструмент самоучёта без лишней аналитики, психологии и «геймификации».

---

## 🎯 Идея проекта

DayMeter помогает:
- ежедневно отмечать ключевые показатели дня (настроение, раздражение, поведение);
- видеть динамику по неделям и месяцам;
- находить связи между нагрузкой, восстановлением и срывами.

Проект ориентирован на **быстрый ввод (30–60 секунд в день)** и **чёткую статистику**, а не на размышления.

---

## 🧱 Технологии

- PHP 8.3
- Laravel
- Blade
- Eloquent ORM
- Chart.js (через CDN, без сборки фронта)
- SQLite / MySQL

Без SPA, без Vue/React, без сложного JS.

---

## 👤 Пользовательская модель

- Стандартная регистрация и авторизация
- Все данные строго привязаны к пользователю
- Нет публичных профилей и шаринга

---

## 🧩 Метрики

Метрики расширяемые, задаются **через код / миграции / сидеры**.

### Поддерживаемые типы
- **boolean** — да / нет
- **scale** — числовая шкала (обычно 1–10)

### Текущий набор метрик

#### Эмоциональное состояние
- **Настроение** (`mood`, scale)

#### Работа
- **Перегруз на работе** (`work_exhausted`, boolean)
- **Работал до поздна** (`worked_at_night`, boolean)

#### Семья и дети
- **Гулял с детьми** (`walked_with_kids`, boolean)
- **Читал детям** (`read_to_kids`, boolean)

#### Активность и восстановление
- **Был на массаже/бассейне/фитнесе** (`was_on_massage`, boolean)
- **Ходьба вечером** (`walked_at_night`, boolean)

---

## 📝 Ввод данных

### `/entry`
- Одна страница для ввода данных
- По умолчанию — **логический текущий день**
- Для каждой метрики:
    - boolean → checkbox
    - scale → number input (min/max)

### Логическая дата дня (важно)
Если ввод происходит **ночью**, день определяется автоматически:

- если текущее время `< 04:00` → запись относится к **предыдущему дню**
- после `04:00` → к текущему

Это позволяет корректно вносить данные перед сном, не искажая статистику.

---

## 📊 Статистика

### `/dashboard`
- Обзорная статистика
- Периоды:
    - неделя
    - месяц
- Агрегаты:
    - среднее по scale-метрикам
    - количество / процент `yes` по boolean-метрикам
    - количество дней с данными

---

### `/stats/month`
Детальная статистика за выбранный месяц.

#### Функционал:
- выбор месяца (`input type="month"`)
- таблица вида:

| Метрика | Значение |
|-------|----------|

- графики:
    - **один график = одна метрика**
    - scale → line chart
    - boolean → bar chart

Графики реализованы через **Chart.js**, без сборки фронта.

---

## 🧠 Архитектура

Основные сущности:
- `Metric`
- `DayEntry`
- `MetricValue`

Бизнес-логика вынесена в сервисы:
- `ReportService`
- `DayResolver` (логическая дата дня)

Принципы:
- нет JSON-хранилищ для значений
- нет дублирования логики
- один сценарий = один URL

---

## ❌ Чего в проекте нет (осознанно)

- AI / рекомендации
- пуш-уведомления
- социальные функции
- редактирование метрик через UI
- сложные интерпретации данных

DayMeter — это **инструмент фиксации и наблюдения**, а не терапия.

---

## 🏁 Кратко

> **DayMeter — это приватный сервис ежедневного учёта состояния, нагрузки и поведения с расширяемыми метриками и наглядной статистикой за неделю и месяц.**

---

## 📌 Статус проекта
Активная разработка для личного использования.  
Архитектура готова к аккуратному расширению без переписывания основы.

# Tools

```
./vendor/laravel/sail/bin/sail up -d
./vendor/laravel/sail/bin/sail artisan migrate
./vendor/bin/sail npm run build

./vendor/bin/sail artisan chronicle:export --from=2026-01-01 --to=2027-01-01 --user-id=2
```
